# 数据删除

删除申请要求近期认证和幂等键。Worker 按 owner 枚举数据库、对象存储和可恢复索引，写入每一步的对象计数与 hash，不记录内容。

执行顺序：冻结新写入 → 撤销会话 → 删除导出/上传对象 → 删除业务关联 → 匿名化必须保留的审计计数 → 写入终态证据。对象不存在视为幂等成功。失败时保留任务为可重试终态，不得对用户显示已完成。备份中的删除按备份保留周期传播，并记录最迟清除日期。
