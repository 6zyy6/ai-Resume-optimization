# 备份恢复

生产数据库启用每日全量与持续 WAL，目标 RPO ≤15 分钟、RTO ≤60 分钟；对象存储启用版本与生命周期清单。每月至少一次在隔离网络恢复。

演练必须记录：备份 ID、创建时间、恢复开始/结束时间、目标提交、迁移版本、行数抽样、owner 隔离查询、对象清单 hash。恢复环境禁止发送邮件、微信通知和真实模型请求。验证完成后销毁隔离环境；未达到 RPO/RTO 记为 `FAIL`。
