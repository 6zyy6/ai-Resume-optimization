# 回滚

应用回滚只切换到上一份不可变镜像，不回写历史镜像标签。先把新流量降为 0，再等待公开请求和 Worker 租约结束。

数据库遵守 expand/contract：发布期只做兼容新增。若迁移包含删除、重命名、收窄类型或不可逆数据变换，不允许自动回滚；恢复旧应用继续读取兼容结构，另行安排修复迁移。回滚后验证 live/ready/version、队列重复投递、Outbox backlog 和导出事实约束。
