# 模型供应商故障

触发条件包括 429/5xx、连接失败、超时或 Pi 失败率连续 10 分钟超过 5%。

1. 通过 trace 核对失败属于供应商而非 schema/权限错误。
2. 启用已审批且数据政策合格的 fallback 路由；不得临时把用户数据发送给未审批供应商。
3. 90% 成本阈值进入降级，100% 停止新 AI 任务；简历读取、事实编辑、已有版本和隐私操作继续可用。
4. 瞬态任务最多重试三次并保留同一 task/run/trace 关联；永久校验失败不得重试。
5. 恢复后逐步放量，检查重复建议、任务终态和成本账本。
