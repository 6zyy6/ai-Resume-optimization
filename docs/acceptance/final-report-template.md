# 发布验收报告

- Release ID：
- Commit SHA：
- Web/API/Pi/Worker 镜像摘要：
- 小程序构建版本：
- 数据库/Prompt/Workflow/模型路由/模板版本：
- 测试环境：
- 执行者 / 独立复核者：

## 结论

仅允许 `PASS`、`FAIL`、`BLOCKED`。任一 P0 `FAIL/BLOCKED` 时结论必须是 `NOT READY`。

## 命令证据

列出命令、开始/结束时间、退出码、原始日志路径和 SHA-256。

## 146 项状态

引用 `manifest.json`，不得手工复制后产生第二事实源。

## 开放缺陷与外部阻断

按 Sev1–Sev4 列出 owner、复现方法、目标修复版本；外部证据写清取得条件。
